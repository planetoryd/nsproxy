[package]
name = "nsproxy-trojan-proto"
version = "0.4.0"
edition = "2024"
license = "GPL-3.0-only"
authors = ["0x72757374 <257538187+0x72757374@users.noreply.github.com>"]
repository = "https://github.com/ple1n/nsproxy"
description = "Trojan protocol parsing and serialization"
keywords = ["trojan", "proxy", "protocol"]
categories = ["network-programming", "parser-implementations"]
readme = "README.md"

[lib]
name = "trojan_proto"
path = "src/lib.rs"

[dependencies]
bytes = "1"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "proto"
harness = false
